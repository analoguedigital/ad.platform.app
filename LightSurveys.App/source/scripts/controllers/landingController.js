'use strict';
angular.module('lm.surveys').controller('landingController', ["$scope", "$rootScope", "$state", '$location', 'authService', 'userService',
    function ($scope, $rootScope, $state, $location, authService, userService) {
        $scope.activate = function () {
            
        }

        $scope.activate();

    }]);